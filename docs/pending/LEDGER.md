# Pending ledger

The record of what `/wow-addon:pending-audit` has already asked about and what was decided, so a
settled item stops re-surfacing on every sweep. **Maintained by `/wow-addon:pending-audit`** — merge,
never clobber.

Each row is matched on **ID + evidence hash** together. The hash is the first 8 characters of `sha1`
over the item's verbatim evidence text, so if the underlying comment, plan row or issue title is
*edited*, the hash changes, the match fails, and the item is correctly interviewed again.

**What counts as "the evidence text"** — pinned down here because it was not, and the 2026-07-31
sweep could not reproduce a single `DOC` or `CODE` hash from the run before it. The algorithm was
never in doubt; the *input* was. A hash nobody can recompute silently turns every settled item back
into an open question, which is the one failure this file exists to prevent. UTF-8, no trailing
newline, `.strip()` applied:

| Source | Evidence text is… |
|---|---|
| `ISS` | the issue title alone — no `#N`, no labels, no body |
| `DOC` | the whole bullet, first line through last continuation line, newlines intact |
| `CODE` | the marker comment and its continuation lines, as written, including the `--` |
| `PLAN` | the plan row or deviation entry as the frozen bundle words it |
| `GIT` | the single `git status --porcelain` / `git stash list` / `git log --oneline` line |

Where a hash in the table below is known not to reproduce from its own evidence, it has been
corrected in place and the correction noted in the rationale. A hash is a lookup key, not history.

Sources: `CODE` (source markers), `DOC` (documentation prose), `PLAN` (audit/review execution plans
and deviation tables), `GIT` (working tree, stashes, commit subjects), `ISS` (GitHub issues), `MEM`
(Claude memory).

## Decision notation

| Marker | Value | Meaning | Re-surfaces? |
|---|---|---|---|
| 🟢 | `done` | Implemented, or otherwise resolved, in the run that recorded it | No — closed |
| 🔵 | `wont-do` | Deliberately closed; the user decided it will never be done | No — closed |
| 🟡 | `deferred` | Not now; still on the books | Yes, as a collapsed count |
| ⚪ | `untriaged` | Found and recorded, but never put to the user | Yes — interviewed in full next run |

Green is the resolved outcome, blue a settled and final close, yellow a decision still asking for
attention, white the absence of any decision at all — a column of yellow is this file telling you
what is left, and a column of white is telling you what nobody has looked at yet. There is
deliberately no red: nothing here is an error state. The **word** is the data and the **marker** is
the affordance, so both are always written; the marker alone is invisible to a screen reader and to
`grep wont-do`.

Three of the four are decisions. **`untriaged` is not** — it records only that an item was seen, and
must never be read as agreement, refusal or postponement. It is the one value that gets fully
re-interviewed rather than collapsed.

The **Issue** column holds the GitHub issue number when the item was deferred with an issue filed,
or when an existing issue was found to already cover it; `—` otherwise. It is what stops the same
pending work being counted twice once the issue comes back through the GitHub sweep.

| ID | Evidence hash | Source | Decision | Issue | Date | Rationale |
|---|---|---|---|---|---|---|
| DOC-03 | 15ebfadf | `docs/ARCHITECTURE.md` ▸ Known limitations | 🟢 done | #6 | 2026-08-03 | Filed as issue #6, then **fixed** on `fix/rename-frames-and-preview-reset`. The limitation bullet this hash was taken over has been deleted, so it cannot re-surface. Root cause was shared with DOC-02: the frame name was derived from the panel name on every read, so a rename had to abandon a frame. It is stamped on the record at create now and a rename leaves it alone, so renaming costs no frames at all. |
| DOC-02 | a531cf09 | `docs/ARCHITECTURE.md` ▸ Known limitations | 🟢 done | #7 | 2026-08-03 | Filed as issue #7, then **fixed** on `fix/rename-frames-and-preview-reset` — and fixed properly rather than by the rename-time notice the issue proposed, which was the best available answer only while "frame names are immutable" was read as "the frame name must track the panel name". It does not: stamping it at create makes a rename a relabel, so external anchors keep working and there is nothing to warn about. Bullet deleted; superseded by the *Frame names and the pool* section. |
| DOC-05 | c296d286 | `docs/ARCHITECTURE.md` ▸ Known limitations | 🟢 done | #8 | 2026-07-31 | Filed as issue #8. The only limitation whose own text proposes the fix; self-contained in the existing ticker. |
| DOC-04 | 10a1babb | `docs/ARCHITECTURE.md` ▸ Known limitations | 🟡 deferred | — | 2026-07-31 | Deferral re-affirmed. Hash moved from `904842e7` because the entry's text was corrected once panel levels became strided — the *limitation* is unchanged (still no level UI, `level` still CLI-only), only its description of cross-panel ordering. |
| DOC-06 | 83f2b5e1 | `docs/ARCHITECTURE.md` ▸ Known limitations | 🟡 deferred | — | 2026-07-31 | Documented design decision. Class color reads `UnitClass("player")` by design. |
| DOC-07 | b724af28 | `docs/ARCHITECTURE.md` ▸ Known limitations | 🟡 deferred | — | 2026-07-31 | Documented design decision — a login-time sweep would silently rearrange a deliberately-parked panel. |
| DOC-01 | d49eaf1f | `docs/ARCHITECTURE.md` ▸ Known limitations | 🟡 deferred | — | 2026-07-31 | Already accepted deviation A-008 in the audit bundle and recorded in the README's Troubleshooting section. Not re-litigated. |
| DOC-08 | dd887a8b | `docs/agent-context.md` (US-English sweep) | 🟡 deferred | — | 2026-07-31 | "Skip this." Left as respelled; still undecided whether the change belongs upstream in WowAddonStandards or the local copy should be restored as vendored text. |
| PLAN-01 | 4a9fdd91 | `docs/audits/2026-07-30/05_EXECUTION_PLAN.md` ▸ Step 0 | 🟡 deferred | — | 2026-07-31 | Release held. Roster registration lives in the WowAddonStandards repo and moves with the release. |
| PLAN-02 | 4b2d92a2 | `docs/audits/2026-07-30/05_EXECUTION_PLAN.md` ▸ Step 3 | 🟡 deferred | — | 2026-07-31 | Release held pending an in-client check of the accent-bar rotation. |
| PLAN-03 | a17b259f | `docs/audits/2026-07-30/02_DEVIATIONS.md` ▸ D-001 | 🟡 deferred | — | 2026-07-31 | Blocked on a CurseForge project id that does not exist until first upload. Not actionable until PLAN-02. |
| PLAN-04 | 089573f0 | `docs/audits/2026-07-30/02_DEVIATIONS.md` ▸ D-002 | 🟡 deferred | — | 2026-07-31 | Same blocker as PLAN-03; the bundle defines the two as one atomic change. |
| PLAN-05 | f99a1c4b | `docs/audits/2026-07-30/02_DEVIATIONS.md` ▸ D-004 | 🟡 deferred | #1 | 2026-07-31 | Bundled with the next live-client session, alongside the accent-rotation check. Tracked by issue #1. |
| PLAN-06 | 4cf84dd8 | `docs/reviews/2026-07-30/05_FINAL_SUMMARY.md` ▸ Known follow-ups | 🟡 deferred | — | 2026-07-31 | "Defer for now." The audit bundle was measured against standard v2.11.0; `performance-§1`–`§4` are MUST as of v2.13.1 with no counterpart in this addon. Deliberately carried, not overlooked. |
| PLAN-07 | 55b62116 | `docs/reviews/2026-07-30/05_FINAL_SUMMARY.md` ▸ Known follow-ups | 🟡 deferred | — | 2026-07-31 | "Keep them open, no plan." Baselines for future comparison, not pass/fail checks; nothing depends on them. |
| GIT-01 | 6d67c2c6 | `git log origin/master..HEAD` | 🟢 done | — | 2026-07-31 | Deferred as "release held — three commits stay local until the in-client check is done", then resolved by the user pushing by hand: `git rev-list --left-right --count origin/master...HEAD` reports `0 0`. Closed, not re-deferred; the deferral was about *when* to push, and the push happened. The branch was `origin/main` when this row was written; renamed to `origin/master` 2026-07-31, locator updated to stay reproducible. Evidence hash unchanged — the locator is not the evidence text. |
| GIT-02 | 28026602 | `git status --porcelain` ▸ `?? docs/pending/` | 🟢 done | — | 2026-07-31 | "Leave it for you to commit." No staging or commit performed here — that is `/wow-addon:commit`'s job. The ledger is reviewed and committed by hand. |
| ISS-01 | 2ebf52d9 | GitHub #1 | 🟡 deferred | #1 | 2026-07-31 | Same item as PLAN-05. Issue stays open as the single tracker; bundled with the next client session. |
| ISS-02 | 0c338158 | GitHub #2 | 🟡 deferred | #2 | 2026-07-31 | "We'll build this later." Filed deliberately as future work, not as a gap. |
| ISS-03 | b0234046 | GitHub #3 | 🟢 done | #3 | 2026-08-03 | Filing the issue *was* the decision — moved out of the frozen review bundle so it is visible in the backlog. **Now implemented** on `fix/rename-frames-and-preview-reset`: `R:Reset` refuses a record carrying `C.PREVIEW_FIELD`, which was the last seam able to strip the marker and promote a placeholder into a permanent panel. Refusal over re-stamping, per the issue's own recommendation. `CopyFrom` needed no guard — `COPY_EXCLUDED` already held the marker. |
| ISS-04 | 4cd5816a | GitHub #4 | 🟢 done | #4 | 2026-07-31 | As ISS-03. |
| ISS-05 | 4120e93f | GitHub #5 | 🟢 done | #5 | 2026-07-31 | As ISS-03. Hash corrected from `49dec1eb`, which does not reproduce from the issue title; the GitHub timeline shows no rename, so the prior value was simply wrong. The decision — filing the issue — is independently verifiable: #5 exists. Not re-interviewed. |
| ISS-06 | f661d445 | GitHub #9 | 🟢 done | #9 | 2026-07-31 | Implemented on `feat/panel-artwork`: catalog, pure `BuildArtSpec` geometry, editor and slash surface, four bundled pieces, 79 tests. Issue closed on GitHub with a comment recording the three departures from the issue as written (blend mode removed, quarter-turns only, per-category art partial). Supersedes the earlier deferral. |
| CODE-01 | 854e4bc3 | `locales/enUS.lua:8-11` | 🟡 deferred | — | 2026-07-31 | "Defer for now." English-only is a stated scope decision for 0.1.0, not an oversight, and the `NS.L` seam is already wired so the pass can happen later without touching call sites. |
| GIT-03 | 9f4990c7 | `git status --porcelain` ▸ `?? modules/Artwork.lua` | 🟢 done | — | 2026-07-31 | "Defer — you commit by hand." Same posture as GIT-02: committing is `/wow-addon:commit`'s job and this command is barred from it. Resolved the same day — the 25 paths carrying the artwork feature landed in `47f507a feat(artwork): draw per-panel artwork inside a panel's bounds`, and the working tree is clean. Closed rather than left deferred: an untracked-file item whose file is now tracked has no pending work behind it. |
| ISS-07 | 7663a77d | GitHub #6 | 🟢 done | #6 | 2026-08-03 | Supersedes the deferral. Fixed on `fix/rename-frames-and-preview-reset`; see DOC-03. The deferral's stated blocker — "a real fix means anonymous pooled frames plus `_G` aliases" — was a false dichotomy: it assumed the frame name must be recomputed from the panel name. Storing it on the record keeps every frame named and anchorable AND stops renames abandoning frames. |
| ISS-08 | b535e138 | GitHub #7 | 🟢 done | #7 | 2026-08-03 | Supersedes the deferral. Fixed on `fix/rename-frames-and-preview-reset`; see DOC-02. "Unfixable (frame names are immutable)" was true of the frame and false of the bug — the frame never needed to change, only the assumption that the panel's name determined it. |
| ISS-09 | 839d9274 | GitHub #8 | 🟡 deferred | #8 | 2026-07-31 | Tracker for a decision already recorded under DOC-05. Self-contained in the shared mouseover ticker whenever it is picked up; the 10Hz interval would need revisiting for an animation. |
| ISS-10 | 9fd018f2 | GitHub #10 | 🟡 deferred | #10 | 2026-07-31 | "Defer — ledger only." Interviewed after the inventory-only pass that first recorded it. Issue #10 already existed and stays open as the single tracker; no duplicate filed. Two gates sit in front of any work here, both named by the issue itself: the resident-memory premise is unmeasured (`UpdateAddOnMemoryUsage` / `GetAddOnMemoryUsage` first), and LoadOnDemand sub-addons have no definition in the Ka0s WoW Addon Standard, so the layout question goes upstream or becomes an accepted deviation before code is written. |
| LIBKA0S-01 | vendor-kit | `../LibKa0s/docs/adoption-prompt.md` step 2 (vendor the test kit) | 🟢 done | — | 2026-08-02 | The shared kit is vendored whole-folder to `tests/_kit/` and `tests/run.lua` is rebuilt on it; `tests/loader.lua` is DELETED in favour of `_kit/loader.lua`. Taken FIRST, before any library module, because `mock_base.lua` is the only source of a `LibStub` with a real `NewLibrary` — without it no vendored `libs/LibKa0s/*.lua` can register headlessly at all and every seam would silently take its degraded path while the suite stayed green. All 16 pre-existing suites kept their exact case counts (564). |
| LIBKA0S-02 | kit-skips-suites | `tests/_kit/framework.lua:89-96` (a listed suite with no file is SKIPPED) | 🟢 done | — | 2026-08-02 | Accepted with a guard rather than a fork. The kit's runner skips a listed suite whose file is absent instead of raising, which this repo's own runner did not do — a silently-green hole. `tests/test_harness.lua` closes it in BOTH directions: listed-but-missing, and on-disk-but-unlisted. Editing `tests/_kit/` to restore the raise was never an option. |
| LIBKA0S-03 | mock-extender | `tests/_kit/mock_base.lua` vs the pre-adoption `tests/wow_mock.lua` | 🟢 done | — | 2026-08-02 | `tests/wow_mock.lua` becomes an EXTENDER over the base rather than being replaced by it. Ten base behaviors are deliberately overridden, each named in that file's header with the reason: the frame stub (the base aliases `CreateTexture`/`CreateFontString` onto the frame itself, which would collapse this addon's four accent edges into one color slot — the base's own header calls this a known divergence it keeps), frames starting shown, AceDB's profile surface, AceAddon's bad-event raise, the bus, the chat capture, Settings, `C_AddOns`/`strtrim`, `UnitClass`/`RAID_CLASS_COLORS`, and LSM staying absent. TAKEN from the base: `LibStub` (real `NewLibrary`, and strict about the silent flag) and the fireable `AceGUI-3.0`. |
| LIBKA0S-04 | acegui-mock | `tests/wow_mock.lua` ▸ the old `AceGUI-3.0 = { Create = function() return nil end }` | 🟢 done | — | 2026-08-02 | This addon's AceGUI mock was a deliberate dead end — `Create` returned nil — on the reasoning that panel bodies build lazily on an OnShow that never fires headlessly. True, and exactly why the schema → widget → write path had NO coverage. The kit's fireable widgets replace it: they record what was set and expose `__fire`, so an OnShow can be fired and `OnValueChanged` driven the way a real click does. This is the prerequisite for testing the Options adoption at all. |
| LIBKA0S-05 | toc-derived-loadlist | `tests/run.lua` (hand-maintained 21-entry load list) vs `_kit/loader.lua` ▸ `tocFiles` | 🟢 done | — | 2026-08-02 | The runner's hand-maintained copy of the load order is deleted; it now derives the addon's files from `PanelMaster.toc`. A second copy of a load order is a second thing that can be wrong, and only one of them is what the client reads — a file added to the TOC but not the list simply never loaded in the suite. `libs\` lines are skipped by `tocFiles` (a vendored library comes in through its own XML), so `tests/run.lua` keeps one explicit six-entry LibKa0s list, Core first. |
| LIBKA0S-06 | core-adopted | `../LibKa0s/docs/adoption-prompt.md` step 4 (Core) + `../LibKa0s/README.md` ▸ the printer descriptor | 🟢 done | — | 2026-08-02 | `LibKa0s-Core-1.0` adopted in `core/CoreSetup.lua` (73 lines). The secret-safe guard and the prefixed printer leave `core/Util.lua` (275 → 241 lines, −34). `NS.Print`, `NS.Util.print`, `NS.SafeToString` and `NS.IsConcatSafe` are republished under those exact names, because SIX files take the printer as a `local print = NS.Print` file-scope upvalue and renaming call sites was never worth it. The descriptor is ONE field — `prefix = NS.PREFIX`, verbatim, as a plain string: `core/Namespace.lua` defines it two files earlier, so the function form is unnecessary here. `sep` and `sink` are deliberately omitted; the default `" "` and `DEFAULT_CHAT_FRAME:AddMessage` are already exactly what this addon did. |
| LIBKA0S-07 | core-chrome-declined | `../LibKa0s/README.md` ▸ `LibKa0s-Core-1.0` (`SKIN`, `ApplySkin`, `MakeCloseButton`) | 🔵 wont-do | — | 2026-08-02 | Core's window-chrome half is DECLINED at the host level, and that is a decision rather than a shortfall. PanelMaster has exactly one standalone window — the debug console — and that window is `LibKa0s-DebugLog-1.0`'s to draw, so it reaches Core's chrome from INSIDE the library. A host-side re-export would have no caller. `NS.Core` is published as the seam if one ever appears. |
| LIBKA0S-08 | core-tripwire | `../LibKa0s/docs/adoption-prompt.md` ▸ the `L` trap, point 2 | 🟢 done | — | 2026-08-02 | Core CANNOT express the `L` trap — it ships no `STRINGS` and reads no descriptor `L` — so a "rendered label is prose" case there would be a case that cannot fail, which is worse than no case because it reads as coverage. The stand-in is a TRIPWIRE on the library: `lib.STRINGS` absent, and `Core.lua`'s source naming neither `STRINGS` nor a `d.L` read. Mutation-verified by adding a `d.L` read to a scratch copy of `libs/LibKa0s/Core.lua` and confirming the case went red with "Core.lua reads a descriptor L"; `libs/` restored from a `cp` backup and re-diffed byte-identical. |
| LIBKA0S-09 | gitattributes | `../LibKa0s/docs/adoption-prompt.md` ▸ "Standing hazards" (CRLF) + this repo's missing `.gitattributes` | 🟡 deferred | — | 2026-08-02 | FLAGGED, not fixed — a deviation from the Ka0s standard that predates this work and is out of its scope. The adoption prompt states "this repo and every addon repo pin `* text=auto eol=crlf`": PanelMaster has NO `.gitattributes` and its working tree is LF throughout, while `../LibKa0s` pins CRLF. The vendored copies therefore land CRLF and BOTH halves of the vendor gate are empty, so nothing is broken today. Adding the file would renormalize ~12,400 lines of unrelated source in the same change as a library adoption, which is the wrong change to bury it in. Wants its own commit. |
| LIBKA0S-10 | debuglog-adopted | `../LibKa0s/docs/adoption-prompt.md` step 4 (DebugLog) + `../LibKa0s/README.md` ▸ the console descriptor | 🟢 done | — | 2026-08-02 | `LibKa0s-DebugLog-1.0` adopted in `core/DebugLogSetup.lua` (208 lines); **`modules/DebugLog.lua` deletes outright (−429)**. Net −221 lines for the same console. Both formatters were ALREADY byte-identical to the library's, `MAX_BUFFER` was already 500, and the frame globals the descriptor derives from `name` (`PanelMasterDebugWindow` / `PanelMasterDebugCopyWindow`) are exactly the two this addon hardcoded — so nothing anchored to them moves. The 25 cases in `tests/test_debuglog.lua` pass unchanged against the library instance, which is the byte-level evidence that the swap is faithful. The file moved from `modules/` up into `core/` because every host reference in the descriptor is a CLOSURE, so nothing is resolved at load and no dependency inverts. |
| LIBKA0S-11 | debuglog-skin | `libs/LibKa0s/Core.lua` ▸ `lib.SKIN` (Core minor 3), `libs/LibKa0s/DebugLog.lua` ▸ `applySkin` | 🟢 done | — | 2026-08-02 | **`applySkin` is DECLINED, and the console's look changes as a result — deliberately.** The old local `SKIN` here painted a plain background texture and declared a `border` color it never actually applied. The console now wears `Core.SKIN`: a flat 1px black edge, a 1px light-gray inner highlight, a gold title and a gray divider. As of Core minor 3 the Ka0s window edge is the LIBRARY's rather than each host's, precisely so the collection's five consoles read as one suite of addons (standalone-windows); taking the hook to preserve this addon's older look is the thing that release exists to undo. USER-VISIBLE — see `docs/smoke-tests.md`. |
| LIBKA0S-12 | debuglog-closebutton | `libs/LibKa0s/DebugLog.lua` ▸ `makeCloseButton` (DebugLog minor 6) | 🟢 done | — | 2026-08-02 | **`makeCloseButton` is DECLINED**, same root cause as LIBKA0S-11 one field along. The console and the copy window wear Core's × (18×18, gray, red on hover) instead of the flat `"X"` this addon drew (gray, gold on hover). DebugLog minor 6 narrowed the field's documentation to a close control that is different in KIND; ours was merely our own. Pinned by asserting `_frameForTest.titleBarOffsets` — the offsets are DERIVED from the button's width, so they are the only readable evidence of which factory built it. USER-VISIBLE. |
| LIBKA0S-13 | debuglog-esc-sync | `modules/DebugLog.lua:304-312` (the old inline `syncPanel`) vs `onVisibilityChanged` | 🟢 done | — | 2026-08-02 | **A bug FIXED by the adoption, recorded so it is not mistaken for drift.** The old console called `syncPanel()` inline from `D:Show()` and `D:Hide()`. The window is registered in `UISpecialFrames`, so Esc hides it WITHOUT going through `D:Hide` — and the settings panel's "Debug console" checkbox has therefore been going stale on every Esc close since the console was written. The library fires `onVisibilityChanged` from the frame's own OnShow AND OnHide hooks, which catches every path including Esc and the close button. |
| LIBKA0S-14 | debuglog-checkbox-declined | `../LibKa0s/README.md` ▸ the `ConsoleCheckbox()` data contract | 🔵 wont-do | — | 2026-08-02 | **`D:ConsoleCheckbox()` is DECLINED.** Its label is byte-identical to this addon's (`Debug console`) but its tooltip is not: ours says the row is SESSION-ONLY and resets on reload, which is a fact about `state.debugConsole` being a `sessionOnly` schema row here and is not something the library can know. The row stays in `settings/Schema.lua` where the rest of the Master Controls live. The contract is still exercised by `tests/test_libka0s.lua` as the `L`-trap rendered assertion, so a regression in the library's own strings is still caught. |
| LIBKA0S-15 | debuglog-tripwire | `../LibKa0s/docs/adoption-prompt.md` ▸ the `L` trap, point 1 | 🟢 done | — | 2026-08-02 | DebugLog is one of the three majors that CAN express the `L` trap, so it gets a REAL rendered assertion rather than a source tripwire: the composed console title, the checkbox label and tooltip, and ten `lib.STRINGS` keys are each read back through a real accessor and asserted not to match `^[A-Z][A-Z0-9_]+$`, with non-vacuity coupling (the title is additionally pinned to its exact bytes, and the tooltip to the resolved `/pm debug` reference) so the case cannot pass by an accessor simply not existing. No `L` is passed: this addon translates nothing. Mutation-verified four ways — a live `L = NS.L`, a 24-wide close button, a drifted frame-global name, and a lost announce-once latch — all four red with the expected message. |
| LIBKA0S-16 | slash-adopted | `../LibKa0s/docs/adoption-prompt.md` step 4 (Slash) + `../LibKa0s/README.md` ▸ the dispatcher descriptor | 🟢 done | — | 2026-08-02 | `LibKa0s-Slash-1.0` adopted in `settings/Slash.lua` (374 → 356 lines; ~150 lines of dispatcher, help renderer, schema CLI and value formatters deleted, ~130 lines of seam and prose added). Moved to the library: `OnSlash`, `PrintHelp`, `HelpRows`, `LandingRows`, `BuildListLines`, `CliList/Get/Set/Reset/ResetAll/Version`, `FormatKV` and the type-aware parser. **Kept host-owned:** `NS.COMMANDS` (see LIBKA0S-18) and every PANEL verb — `CliNew`, `CliDelete`, `CliRename`, `CliPanels`, `CliPanel`, `CliRecover`, `BuildPanelLines`, `BuildPanelShowLines` — which act on registry records rather than schema rows, so the library's schema CLI has nothing to say about them. |
| LIBKA0S-17 | slash-rendered-changes | `settings/Slash.lua` rendered output vs `libs/LibKa0s/Slash.lua` | 🟢 done | — | 2026-08-02 | **The rendered-output changes a user will notice, accepted knowingly.** (1) Every command row and every `key = value` row now uses UPPERCASE color escapes (`|cFFFFFF00`) where this addon used lowercase — WoW's escape parser is case-insensitive, so the pixels are identical and only the source bytes moved. (2) The help header gains an em dash: `v0.1.0 slash commands (…)` → `v0.1.0 — slash commands (…)`. (3) `/pm help` rows are now INDENTED two spaces, because each sits under a header in chat. (4) A parse failure is now TWO lines — `Invalid value for <path>` then the reason, indented — where it used to be one. (5) An out-of-range number CLAMPS to the row's max instead of being refused with `error: invalid value`; the echo re-reads the stored value, which is the only thing that makes a clamp honest rather than silent. All five are pinned by cases in `tests/test_slash.lua`. |
| LIBKA0S-18 | commands-positional | `settings/Slash.lua` ▸ `NS.COMMANDS` | 🟢 done | — | 2026-08-02 | `NS.COMMANDS` flips from keyed `{ name =, desc =, fn = }` to the positional `{ name, description, handler }` triples the library reads. Eighteen entries, the dispatcher, the help renderer, the settings landing page and seven test cases moved together. The table itself STAYS this addon's and is passed in rather than owned — the landing page renders the same rows, and a library that owned the table would force the options major to resolve the slash major to read it, which is a real dependency cycle between two majors at load time. |
| LIBKA0S-19 | schema-vocabulary | `settings/Schema.lua` row types and option lists | 🟢 done | — | 2026-08-02 | The schema's vocabulary moves to the one LibKa0s reads: `type = "boolean"` → `"bool"` (6 rows) and `options` → `values` (1 row). An unmapped field here is not an error but something worse — a `set` that answers `unknown setting type 'boolean'` and, once Options lands, a row that silently vanishes from the page. `C.STRATA_OPTIONS` gains a `text` key alongside its existing `label`: `text` is what both LibKa0s majors read, `label` is what `settings/PanelEditor.lua` reads for the dropdowns it draws by hand, and writing both means neither reader has to know about the other. |
| LIBKA0S-20 | list-group-order | `settings/Slash.lua` ▸ the old `LIST_GROUP_ORDER` constant | 🔵 wont-do | — | 2026-08-02 | The hand-maintained `/pm list` group-order constant is **deleted rather than ported**. It named the three schema groups in the order they are already declared in, which is pure duplication, and its own comment admitted the failure mode: "a name that matches nothing fails invisibly". LibKa0s-Slash-1.0 groups in DECLARATION order outright, on the reasoning that a schema's own order is the order its panel shows. The two cases that guarded the constant are replaced by cases asserting the property directly against the rendered listing. |
| LIBKA0S-21 | slash-parse-adapter | `settings/Slash.lua` ▸ the descriptor's `parse` + `groupKey` | 🟢 done | — | 2026-08-02 | Two ADAPTERS, both host-shaped rather than library gaps, so both are closures in the seam file. (1) `groupKey` — the library groups `/pm list` by `row.page`; this addon's schema has always grouped by `row.group`, which is also the section heading its panel draws. (2) `parse` — `lib.ParseValue` matches an enum case-sensitively, which is right for a texture name and wrong for this addon's one enum: strata tokens are stored upper-case and `/pm set settings.defaultStrata low` has always worked. The adapter up-cases only for a row that actually declares an enum, then delegates, so every other type keeps the library's parser including the number clamping and the color rescaling this addon never had. **A vacuous pre-existing test was found here**: the lower-case-token case set `low` against a row whose default is already `LOW`, so it passed whether the adapter existed or not; a mutation deleting the adapter left it green. Rewritten to move off the value first. |
| LIBKA0S-22 | convergence-1 | `../LibKa0s/docs/adoption-prompt.md` convergence #1 (`reset` takes a path) | 🟢 done | — | 2026-08-02 | **ADOPTED — and it was already converged, which is not the same as "not applicable".** `/pm reset <path>` was path-scoped and `/pm resetall` was the global form before the adoption, so nothing user-visible changed; both now delegate to `Sl:CliReset` / `Sl:CliResetAll`. There is no page-shaped form and there never was, so no working command was removed and no destructive path lost a confirmation. The one destructive verb this addon has, `/pm panel deleteall`, is not a schema reset at all and keeps its `KA0S_PANELMASTER_DELETEALL` popup on both entry points (the slash verb and the Panels page's Defaults button). Recorded rather than left implicit, because an unrecorded decision is indistinguishable from an oversight. |
| LIBKA0S-23 | convergence-2 | `../LibKa0s/docs/adoption-prompt.md` convergence #2 (one command-row formatter) | 🟢 done | — | 2026-08-02 | **ADOPTED, and it is user-visible.** `settings/Panel.lua`'s landing page carried a SECOND formatter for `NS.COMMANDS` beside the chat one in `settings/Slash.lua`, and the two had already drifted: the landing one put double spaces either side of the em dash, wrapped the dash itself in white and left the description bare. Both now come from `lib.FormatRow` via `Sl:LandingRows()`. The double spaces collapse to single, the dash loses its color span and the description gains one. Proved by following the RENDER PATH rather than by grepping for a name — `buildMainContent` is where the page's rows are drawn. |
| LIBKA0S-24 | options-adopted | `../LibKa0s/docs/adoption-prompt.md` step 4 (Options) + `../LibKa0s/README.md` ▸ the panel descriptor | 🟢 done | — | 2026-08-02 | `LibKa0s-Options-1.0` adopted. `settings/OptionsSetup.lua` (135 lines) holds the descriptor and `NS.Helpers`, which **IS** the library instance rather than a wrapper around one (options-ui-§1) — which is what lets `settings/Panel.lua` decorate it in place. `settings/Panel.lua` goes 691 → 492 lines, and a large share of what remains is prose recording what moved. Taken outright: `CreatePanel` (with the header, the breadcrumb and the Blizzard canvas contract), `EnsureDefaultsButton`, `EnsureScroll`, `PatchAlwaysShowScrollbar`, `Section`, `AddSpacer`, `AttachTooltip`, `ClearScroll`, `RenderField`, `RenderSchema`, `SetRenderer`, `RegisterOptionsPage`, `CreateOptionsPanel`, `OpenOptionsPanel`, `RefreshScalars` and `lib.LAYOUT`. |
| LIBKA0S-25 | options-adapters | `settings/Panel.lua` ▸ the two instance wraps | 🟢 done | — | 2026-08-02 | Two ADAPTERS, both wrapped ON THE INSTANCE rather than written as host-side helpers beside it, and the distinction is load-bearing: `O.RenderRows` resolves `O.RenderField` and `O.EnsureScroll` from the instance TABLE at call time, so a plain local helper is bypassed by every page the flow engine draws. (1) `RenderField` — the open-dropdown registry is this addon's own invention, born of a real bug, and no other host has one; the wrap catches every dropdown the library builds. (2) `EnsureScroll` — the library's scrollbar patch has no notion of closing an open dropdown on scroll, so this addon's `MoveScroll` layer and scrollbar `OnMouseDown` hook are installed OVER the library's, never replacing them. **A test caught the second one for real**: written first as a host-side helper, the hooks landed on the landing page and the panel editor and were missing from the one page that actually has a dropdown on it. |
| LIBKA0S-26 | options-declined | `../LibKa0s/LibKa0s/Options.lua` ▸ `RestoreAllDefaults`, `InlineButtonPair`, `LSMValues`, the color codecs | 🔵 wont-do | — | 2026-08-02 | Four library surfaces knowingly DECLINED, each for a reason rather than by omission. (1) `RestoreAllDefaults` — this addon's global reset must ALSO reach the session-only rows (unlock, preview, the console) through each row's own `set`; the library's version walks `allRows` calling `applyDefault`, which for a `sessionOnly` row writes a default the row does not store. `Sl:CliResetAll` stays the one implementation both the slash verb and the Defaults button share. (2) `InlineButtonPair` — it builds and owns its own Flow row; this addon's paired buttons are added INTO a row somebody else is building (the companion beside Grid size, the Reset/Delete pair inside the panel editor), so a maker that owned the row would have nowhere to put them. `makePairButton` stays host-owned. (3) `LSMValues` and (4) `colorDecode`/`colorEncode` — no schema row here is media-backed or color-typed; both live on panel RECORDS, which `settings/PanelEditor.lua` draws by hand from `NS.Registry`. Neither would have a caller. |
| LIBKA0S-27 | options-rendered-changes | `settings/Panel.lua` rendered output vs `libs/LibKa0s/Options.lua` | 🟢 done | — | 2026-08-02 | Two rendered changes a user will notice. (1) The combat refusal on `/pm config` is `|cffaaaaaa` where this addon used `|cff808080` — the sentence is byte-identical, only the shade of gray moved, and it moved lighter because the string is now one implementation shared with every other Ka0s addon rather than six copies. (2) The header Defaults button gains a TOOLTIP on both pages, which it never had, because `O.EnsureDefaultsButton` attaches `opts.defaultsTooltip`. Also structural rather than visual: the main canvas is now NAMED (`PanelMasterOptionsPanel`), where the old `createPanel` passed nil and left every one of this addon's canvases anonymous and unattributable to `/framestack`. |
| LIBKA0S-28 | options-onDefault | `tests/test_panel.lua` ▸ "the header Defaults action and Blizzard's OnDefault" | 🟢 done | — | 2026-08-02 | **An assertion changed to accommodate a deliberate library behavior, recorded because changing a test to match a change is the move that deserves saying out loud.** The case asserted `panel.defaultsOnClick == panel.OnDefault` — the same OBJECT. `O.CreatePanel` stamps `OnDefault` as a FORWARDER that resolves `defaultsOnClick` at CALL time, because every host parks its handler AFTER `CreatePanel` returns (the button does not exist yet, being built on first OnShow) and a plain assignment there would capture nil forever while looking correct. The assertion moves from identity to behavior: firing Blizzard's footer control must run the same closure the header button runs, which is the property that actually mattered. |
| LIBKA0S-29 | options-render-coverage | `tests/test_libka0s.lua` ▸ the Options render cases | 🟢 done | — | 2026-08-02 | **The schema → widget → write path had NO coverage before this adoption and now has eight cases.** This addon's own AceGUI mock returned nil from `Create()`, deliberately, on the reasoning that panel bodies build lazily on an OnShow that never fires headlessly — which was true, and which is exactly why nothing was tested. The kit's fireable widgets make it reachable: a page's OnShow is fired, the widgets the library built are found by type and label, and `OnValueChanged` / `OnMouseUp` are driven the way a real click drives them. **A mutation found dead code here too**: the General page rendered through `RenderRows` with an explicit row list, which worked identically and left the descriptor's `rowsForPage` never called — emptying it changed nothing and failed no case. Switched to `RenderSchema`, which routes through it. |
| LIBKA0S-30 | options-tripwire | `../LibKa0s/docs/adoption-prompt.md` ▸ the `L` trap, point 2 | 🟢 done | — | 2026-08-02 | Options is the other major that cannot express the `L` trap, and its tripwire is deliberately NOT Core's copied across: `Options.lua` ships its own `lib.STRINGS`, so `rawget(lib, "STRINGS") == nil` would fail there against a module behaving exactly as designed. The source half alone is the tripwire — all THREE Options files swept for a `d.L` read — and `lib.LAYOUT` is pinned alongside `lib.STRINGS` so that `local L = lib.LAYOUT` inside that file is understood as geometry rather than mistaken for a locale. Mutation-verified by adding a `d.L` read to a scratch copy of `libs/LibKa0s/OptionsWidgets.lua`; `libs/` restored from a `cp` backup and re-diffed byte-identical. |
| LIBKA0S-31 | perf-declined | `../LibKa0s/README.md` ▸ `LibKa0s-Perf-1.0` + `modules/Canvas.lua:450-490` | 🔵 wont-do | — | 2026-08-02 | **`LibKa0s-Perf-1.0` DECLINED, and the reason is what the addon does rather than a judgement about thresholds.** Perf measures two combat-gated windows over one fight, differing only in whether the host is inert. PanelMaster draws static backdrop panels: every event it registers is `PLAYER_LOGIN`, `PLAYER_ENTERING_WORLD` or `PLAYER_REGEN_ENABLED` — none of which fires *during* combat — and every repaint path (`Canvas:RenderAll`, `Canvas:Render`) is driven by a settings change or a panel edit, i.e. by the user having the options panel open, which options-ui-§2 refuses in combat outright. So every bucket but one would read `0.000` by construction. The single exception is the shared mouseover ticker (`modules/Canvas.lua:459-486`): 10 Hz, one `MouseIsOver` and one `SetAlpha` per *mouseover-tracked* panel, a per-panel opt-in that is **off by default**. A capture whose report is one bucket, near the noise floor, on a feature most installs never enable, tells nobody anything — and buying it would mean putting a `Perf.suspended` check inside the renderer's own show decision, since the host contract requires visibility be enforced at the source rather than by imperatively hiding frames. Real code in the render path for a diagnostic that cannot answer a question. Revisit if a genuinely combat-time hot path ever appears. |
| LIBKA0S-32 | docs | `../LibKa0s/docs/adoption-prompt.md` steps 9 and 10 | 🟢 done | — | 2026-08-02 | Docs moved with the code rather than after it. `docs/testing.md` gains **The vendor gate** — the four diffs, run in pairs, with a table saying what each of the three possible answers means and what to do, because neither green gate can see a stale vendored copy: the library's suite passes against the library and this addon's passes against a stale copy that still works. `docs/ARCHITECTURE.md`'s module map gains `core/CoreSetup.lua`, `core/DebugLogSetup.lua` and `settings/OptionsSetup.lua`, loses `modules/DebugLog.lua`, and records `NS.LIBKA0S_MISSING` as a **cross-file contract three other seams depend on** rather than an implementation detail of one file. `docs/smoke-tests.md` gains four sections (§14–§17) for what no headless suite can reach: the degraded install, the `L`-trap sweep, the nine deliberate rendered changes with a parity list beside them, and the destructive path's confirmation on **both** entry points. `README.md` names the bundled version, and the `[tests]` badge and `docs/test-cases.md` moved together in every commit. |
| ARTWORK-01 | wiki-art-licensing | `docs/artwork-spec.md` ▸ Licensing ("Do not submit ripped or traced Blizzard art") | 🟢 done | — | 2026-08-02 | **The spec's one hard prohibition, deviated from knowingly.** All 104 bundled pieces are Warcraft Wiki plates — Blizzard-owned class crests, race and faction emblems, expansion logos — which the spec says outright "cannot ship" under an MIT release. Raised BEFORE the import was built rather than discovered after, and the user confirmed it as a deliberate call: the alternative was 104 commissioned or generated plates for an addon whose entire subject is Warcraft UI. What is traded away is the clean redistribution story — this addon is no longer MIT end-to-end, and a takedown or a CurseForge asset-policy objection is a live risk that has to be answered by pulling `media/artwork/`, not by swapping a license header. The `credit` field on every row reads `Warcraft Wiki (Blizzard Entertainment)`, so the provenance is stated on the record rather than laundered; the spec's attribution requirement is met even though its licensing requirement is not. Reconsider if the addon is ever submitted anywhere that reviews asset provenance. |
| ARTWORK-02 | 1024-over-size-cap | `docs/artwork-spec.md` ▸ Technical requirements (`File size — Under ~1.5 MB`, `Recommended size 512 × 512`) | 🟢 done | — | 2026-08-02 | **1024 × 1024 chosen over 512 × 512, which puts every file over the spec's size cap.** RLE-compressed 32-bit TGA averages ~1.72 MB per plate against a stated ceiling of ~1.5 MB, and the pack totals ~179 MB where the 512 option came in at 48.5 MB. Bought: plates that hold up at full-panel scale, which is the size a backdrop is actually drawn at — a 512 source visibly softens once a panel is larger than its own art, and these are upscaled wiki plates rather than authored-at-size emblems, so the headroom is the whole point. Traded away, in two currencies. **Download and repo size**: ~179 MB is a large addon by any measure and the number a player sees. **VRAM, which the disk figure hides**: RLE compresses the *file*, not the texture — the client decompresses to raw RGBA, so every distinct piece in use costs 1024 × 1024 × 4 = **4 MB of video memory**, four times the 512 equivalent, and a layout using a dozen different plates is 48 MB resident. The spec's cap was written as a proxy for exactly this ("anything much larger means the dimensions are wrong"), so the deviation is against its reasoning and not merely its number. |
| ARTWORK-03 | dropped-bw-color-suffix | `docs/artwork-spec.md` ▸ Naming and registration ("The suffix is mandatory even when only one half of the pair exists") | 🟢 done | — | 2026-08-02 | **The mandatory `-bw` / `-color` suffix is dropped** in favour of a layout that mirrors the source tree — `class/warrior.tga`, `faction/expansion/12-midnight/harati.tga`. The suffix exists to make tint behavior readable from a file listing, and with 104 assets the folder tree is the organising axis that actually pays: a flat directory of 104 suffixed stems is unnavigable, and every one of these plates is `tintable = false`, so a uniform `-color` on all 104 would be noise carrying no information. Traded away exactly what the spec warns about — **tint behavior is no longer visible in a listing, and the catalog row's `tintable` field is the sole truth**. That is tolerable only while the answer is uniformly false; the moment a single tintable plate is added, this either goes back to suffixes or the pair is distinguished some other way, and whoever adds it must read this row first. No addon code moves: `modules/Artwork.lua:128` already composes `C.ARTWORK_PATH_PREFIX .. row.file .. ".tga"`, so the `file` stem simply carries the backslash-separated subfolders (`faction\expansion\12-midnight\harati`) the client expects. |
| ARTWORK-04 | vendored-upscaler | `tools/artwork/bin/` (~20 MB) + `.pkgmeta` ▸ `ignore: tools` | 🟢 done | — | 2026-08-02 | Recorded because ~20 MB of binary in a Lua addon repo looks like a mistake and is not. The upscaler the wiki plates are run through is **vendored at `tools/artwork/bin/`** rather than fetched, so the import is reproducible from a clone alone — a pipeline whose one irreplaceable step is a download link is a pipeline that stops working the day the link rots, and re-deriving 104 assets from a different binary would not be byte-reproducible. `.pkgmeta` already ignores `tools` outright ("build-time only, and not even Lua"), so this costs **repo size and clone time, never player download** — it is not in the packaged addon at any version. Distinct from ARTWORK-02, which *is* shipped weight. Not covered by the vendor gate: `libs/` and `tests/_kit/` are LibKa0s copies with an upstream to diff against, and this binary has no such source of truth. |
