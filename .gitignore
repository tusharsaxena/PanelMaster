# Python bytecode from tools/artwork/. The importer itself is committed (it is how authored and
# contributed art is converted to the TGA the client loads); its compiled cache is not.
__pycache__/
*.pyc
