# Candidates (PanelMaster)

None. v1.62.0 adds no public surface: its four new files are moves of existing members, and its kit
revisions change the runner's output and file layout only. Everything arrives on the copy (class A):
the `None.` row under an empty watch-list table (ATS-20) and the generated-file carve-out in the band
table (ATS-21) take effect on this repo's next `run-automated-tests.sh` bundle.
