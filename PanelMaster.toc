## Interface: 120100
## Title: Ka0s Panel Master
## Notes: Draws plain backdrop panels behind your UI, so separate frames read as deliberate groups.
## Author: add1kted2ka0s
## Version: 1.1.1
## IconTexture: Interface\AddOns\PanelMaster\media\logos\panelmaster.logo.128.tga
## SavedVariables: PanelMasterDB
## OptionalDeps: Ace3, LibStub, CallbackHandler-1.0, LibSharedMedia-3.0, AceGUI-3.0-SharedMediaWidgets, LibDataBroker-1.1, LibDBIcon-1.0
## DefaultState: enabled
## Category-enUS: UI
## X-License: MIT
## X-Standard: https://github.com/tusharsaxena/WowAddonStandards
## X-Curse-Project-ID: 1642836

# Libraries (vendored in libs/ — load first)
libs\LibStub\LibStub.lua
libs\CallbackHandler-1.0\CallbackHandler-1.0.xml
libs\AceAddon-3.0\AceAddon-3.0.xml
libs\AceEvent-3.0\AceEvent-3.0.xml
libs\AceTimer-3.0\AceTimer-3.0.xml
libs\AceConsole-3.0\AceConsole-3.0.xml
libs\AceDB-3.0\AceDB-3.0.xml
libs\AceGUI-3.0\AceGUI-3.0.xml
libs\AceConfig-3.0\AceConfig-3.0.xml
libs\AceDBOptions-3.0\AceDBOptions-3.0.lua
libs\LibSharedMedia-3.0\lib.xml
libs\AceGUI-3.0-SharedMediaWidgets\widget.xml
# The launcher's two libraries (launcher-§1), AFTER CallbackHandler-1.0 and LibStub, which
# LibDataBroker resolves at its own file load. LibDBIcon reads the broker object this addon
# registers, so the pair has to be present before core\LauncherSetup.lua runs; both are
# resolved with LibStub(..., true) there, and a client missing either degrades by name.
libs\LibDataBroker-1.1\LibDataBroker-1.1.lua
libs\LibDBIcon-1.0\LibDBIcon-1.0.lua
# LibKa0s AFTER LibStub and after Ace3: Core resolves LibStub, and DebugLog/Slash/Options each
# resolve LibKa0s-Core-1.0 before calling NewLibrary. Vendored whole-folder from ../LibKa0s.
libs\LibKa0s\LibKa0s.xml

# Locales
locales\enUS.lua
locales\PostLoad.lua

# Core (Compat loads first)
core\Compat.lua
# The LibKa0s-Env-1.0 seam, immediately after Compat because it is what took the TOC-metadata
# reader out of it. Its position is conventional, not load-bearing: nothing here resolves at file
# load, and NS.version is read at call time from core/Namespace.lua, which loads later.
core\EnvSetup.lua
# MediaSetup BEFORE Constants, and that position is load-bearing rather than conventional:
# core/Constants.lua resolves C.FONT_MONO from NS.MediaFont at FILE LOAD, so a seam published
# later would leave the constant holding nil forever. It needs nothing but its own addonName
# vararg and LibStub, both of which are already here.
core\MediaSetup.lua
core\Constants.lua
# Conventional from here through State: Namespace and State only publish tables and constants, and
# nothing in either reads another file at load. Namespace has to precede CoreSetup (NS.PREFIX, noted
# below); where it sits before that is convention.
core\Namespace.lua
core\State.lua
# Util AFTER Constants, load-bearing: core/Util.lua:4 captures `local C = NS.Constants` at file
# scope, so Util loading first would hold nil forever.
core\Util.lua
# CoreSetup AFTER Namespace (NS.PREFIX) and Util (NS.Util), and BEFORE PanelMaster.lua, whose
# AceConsole embed reclaims NS.Print from NS.Util.print. See the header of the file.
core\CoreSetup.lua
# The LibKa0s-Bus-1.0 seam, and its position is LOAD-BEARING: modules\Registry.lua and
# settings\Schema.lua call NS.BusLib.Catalog at FILE LOAD, so it must load before both. It needs
# nothing but LibStub.
core\BusSetup.lua
# The LibKa0s-DebugLog seam. After Constants (C.FONT_MONO) and after CoreSetup
# (NS.LIBKA0S_MISSING). Everything else it touches is reached through a closure, so the console no
# longer has to sit down in modules/ the way modules/DebugLog.lua did.
core\DebugLogSetup.lua
# The LibKa0s-Lifecycle-1.0 seam: the stand-down latch (slash-commands-§7). After DebugLogSetup for
# NS.Debug, which NS.StandDown and NS.StandUp both trace through; everything else it touches --
# NS.db, NS.Schema, NS.Canvas, NS.addon -- is reached through a closure and resolved at call time,
# so the rest of its position is conventional. Nothing calls the latch until addon:OnEnable.
core\LifecycleSetup.lua
# Conventional from here through Database, beyond the one pin CoreSetup notes above (PanelMaster.lua
# after CoreSetup and Util): Database defines functions only, and NS:InitDB reads NS.defaults at
# call time, from OnInitialize.
core\PanelMaster.lua
core\Database.lua
# The LibKa0s-Launcher-1.0 seam: one LibDataBroker object, registered twice (launcher-§1). After
# CoreSetup (NS.LIBKA0S_MISSING) and after Constants (C.ICON_PATH); everything else it touches --
# NS.db, NS.Schema, NS.Panel -- is reached through a closure and resolved at call time, so the rest
# of its position is conventional. Register() runs from OnInitialize, after NS:InitDB().
core\LauncherSetup.lua

# Defaults -- conventional from here: both files only fill NS.defaults, which nothing reads until
# NS:InitDB runs from OnInitialize.
defaults\Profile.lua
defaults\Global.lua

# Modules (Registry before Canvas — the renderer reads the registry). The notes below pin
# Registry, Artwork, SunnArtPacks and SunnArt ahead of Canvas; Unlock is conventional: it captures
# only core/ tables at file scope and reaches Canvas at call time.
modules\Registry.lua
# Artwork before Canvas — the renderer reads its art list and geometry math
modules\Artwork.lua
# The known-pack manifest, before the adapter that reads it. Pure data, no behavior: what the
# official Sunn packs contain, so the feature survives SunnArt itself being disabled or unloadable.
modules\SunnArtPacks.lua
# The Sunn - Viewport Art adapter, AFTER Artwork (it appends to Artwork.Catalog) and before Canvas.
# Discovery only: it reads globals that user-installed pack addons leave behind and ships no art of
# its own. Silent when nothing Sunn-shaped is installed.
modules\SunnArt.lua
modules\Canvas.lua
modules\Unlock.lua
modules\Diagnostics.lua

# Settings (last — depend on everything else being initialized)
settings\Schema.lua
# Slash AFTER Schema, load-bearing: its dispatcher descriptor takes NS.SchemaRuntime's members as
# VALUES at file load (settings/Slash.lua:601-605).
settings\Slash.lua
settings\PanelEditor.lua
# The LibKa0s-Options seam. After Schema.lua (the descriptor reads it) and Slash.lua (the landing
# page renders through Sl:LandingRows), and BEFORE Panel.lua, which captures the instance at file
# scope. PanelEditor.lua binds its helpers lazily inside its own rebuild, so it pins nothing here.
settings\OptionsSetup.lua
settings\Panel.lua
